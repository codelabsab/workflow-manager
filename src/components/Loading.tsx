import React from "react";

const Loading = () => {
  return (
    <div className="sm:mt-20sm:text-4xl mt-10 flex animate-pulse items-center justify-center text-xl font-semibold text-gray-500">
      Loading...
    </div>
  );
};

export default Loading;
